{
  "name": "smt-bnb-bridge",
  "version": "6.1.0",
  "private": true,
  "description": "SMTAgentRegistry — on-chain identity + reputation + AI decision record for SMT on BNB Smart Chain (BNB Hack: AI Trading Agent Edition).",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bscTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bsc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
